function imgSlider(newImg) {
    const pepsi = document.querySelector('.pepsi').src = newImg;

};

function backColour(bgColor) {
    const sec = document.querySelector('.sec');
    sec.style.background = bgColor;
};

function tColor(textColor) {
    const specificLink = document.querySelector('.btn');
    specificLink.style.color = textColor;
};